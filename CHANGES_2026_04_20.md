# 修复和更新总结

## 2026-04-20 更新

### ✅ 已完成的任务

#### 1. 将 floris-4.6.4 排除在 git 之外
- 更新了 `.gitignore` 文件，添加 `/floris-4.6.4` 条目
- Python FLORIS 参考代码现在不会被追踪

#### 2. 修复多次调用 set_wind_conditions 引起的错误

**问题描述**：
之前在同一程序中多次调用 `set_wind_conditions()` 改变 n_findex 时，会导致索引越界错误。这是因为：
- Grid 被重置为 None 并重新初始化
- 但 `Core.state.initialized` 仍然为 true
- 导致 `initialize_domain()` 没有被调用
- Farm 的 sorted 数组没有正确更新

**修复方案**：
在 `Core::initialize_grid()` 方法的最后添加了：
```rust
// Mark as uninitialized since grid changed - will be reinitialized on next run()
self.state.initialized = false;
```

这样当 grid 重新初始化后，下次 `run()` 时会正确地重新调用 `initialize_domain()`，从而更新所有 farm 的 sorted 数组。

**测试验证**：
创建了测试示例 `test_multiple_set_wind_conditions.rs`，验证了可以安全地多次调用 `set_wind_conditions()` 切换不同的工况（3个 → 1个 → 4个 findex）。

#### 3. 合并分开的示例

**合并的示例**：
- `005_getting_power.rs` + `005b_getting_power_windrose.rs` → `005_getting_power.rs`
  - 现在单个示例同时演示 TimeSeries 和 WindRose 条件
  - 使用独立的 FlorisModel 实例避免状态冲突

**删除的文件**：
- `examples/005b_getting_power_windrose.rs` （已合并到 005）
- `examples/test_multiple_set_wind_conditions.rs` （临时测试文件）

#### 4. 实现完整的示例

**007_sweeping_variables.rs** - 现在包含所有扫描类型：
1. ✅ 风速扫描（5.0-9.9 m/s）
2. ✅ 风向扫描（250°-289°）
3. ✅ 湍流强度扫描（0.03-0.19）
4. ✅ 偏航角扫描（-30° to 30°）

每个扫描类型使用独立的 FlorisModel 实例，确保状态隔离。

### 📊 测试结果

所有示例均已通过测试：
```
✅ 001_opening_floris_computing_power
✅ 004_set
✅ 005_getting_power (合并版)
✅ 006_get_farm_aep_simple
✅ 007_sweeping_variables (完整版)
✅ 008_uncertain_models
✅ 009_parallel_models
✅ 010_compare_farm_power_with_neighbor
```

### 🔧 技术细节

**关键修改文件**：
1. `.gitignore` - 添加 floris-4.6.4 排除
2. `src/core/core.rs` - 修复 initialize_grid 中的状态管理
3. `src/floris_model.rs` - set_wind_conditions 中标记 farm 为未初始化
4. `examples/005_getting_power.rs` - 合并 WindRose 示例
5. `examples/007_sweeping_variables.rs` - 实现完整扫描功能
6. `examples/README.md` - 更新文档

**核心修复逻辑**：
```rust
// In Core::initialize_grid()
pub fn initialize_grid(&mut self) -> crate::Result<()> {
    // ... grid initialization code ...
    
    // Mark as uninitialized since grid changed - will be reinitialized on next run()
    self.state.initialized = false;
    
    Ok(())
}
```

这个简单的修改确保了每次 grid 变化后，farm 的所有 sorted 数组都会在下次运行时正确更新。

### 📝 更新后的示例列表

| 示例 | 功能 | 状态 |
|------|------|------|
| 001 | 基本功率计算 | ✅ 完整 |
| 004 | 设置方法演示 | ✅ 完整 |
| 005 | 获取功率（TimeSeries + WindRose） | ✅ 完整（已合并） |
| 006 | AEP 计算 | ✅ 完整 |
| 007 | 变量扫描（4种类型） | ✅ 完整（已扩展） |
| 008 | 不确定性模型 | ⚠️ 简化版 |
| 009 | 并行模型 | ⚠️ 概念演示 |
| 010 | 邻近风电场对比 | ✅ 完整 |

### 🎯 下一步计划

1. 实现完整的 UncertainFlorisModel 类
2. 实现 ParFlorisModel 支持真正的并行计算
3. 添加 turbine_weights 参数支持
4. 实现 power setpoints 和 derating 功能
5. 添加更多可视化示例

---

**修复日期**: 2026-04-20  
**影响范围**: 核心状态管理、示例代码  
**向后兼容性**: ✅ 完全兼容，仅修复 bug
